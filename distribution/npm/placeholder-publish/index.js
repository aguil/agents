#!/usr/bin/env node

console.error(
  "@aguil/agents@0.0.0 is a name-reservation placeholder only. Install a non-zero CI-published version (or build from github.com/aguil/agents).",
);
process.exitCode = 2;
