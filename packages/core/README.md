# Core

Shared lifecycle, event, finding, and run-result contracts used by all harnesses.

Also provides repository-path helpers such as `resolveGitAwarePath()`, which maps a jj workspace path to its canonical colocated repo for git/gh commands.
