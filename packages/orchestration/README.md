# Orchestration

Harness orchestration contracts and native Bun orchestration utilities. Keep this boundary stable so a framework such as Mastra can be introduced later if the workflow graph outgrows the native implementation.
