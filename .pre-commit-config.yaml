# Markdown formatting: prettier (.editorconfig + .prettierrc.json, .prettierignore).
# https://pre-commit.com
repos:
  - repo: local
    hooks:
      - id: prettier
        name: prettier (markdown)
        entry: mise exec --locked -- prettier --check --ignore-unknown
        language: system
        types: [markdown]
